using Knapsack.Core;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Knapsack.Test
{
    [TestClass]
    public class ParserTests
    {
/*
        [TestMethod]
        public void GivenParameters_ParserParse_CreatesKnapsackProblem()
        {
            // Arrange (Подготовка)
            string parameters = "4\n2 3 4 5\n12\n2 3 4 1\n3 2 1 3\n";
            Parser parser = new Parser();

            // Act (Действие)
            KnapsackProblem problem = parser.Parse(parameters);

            // Assert (Проверка)
            Assert.AreEqual(4, problem.N);

            Assert.AreEqual(12, problem.B);

            Assert.AreEqual(2, problem.A[0]);
            Assert.AreEqual(3, problem.A[1]);
            Assert.AreEqual(4, problem.A[2]);
            Assert.AreEqual(5, problem.A[3]);

            Assert.AreEqual(new Vector(2, 3), problem.C[0]);
            Assert.AreEqual(new Vector(3, 2), problem.C[1]);
            Assert.AreEqual(new Vector(4, 1), problem.C[2]);
            Assert.AreEqual(new Vector(1, 3), problem.C[3]);
        }
*/
    }
}
