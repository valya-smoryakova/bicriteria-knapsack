using Knapsack.Core;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Knapsack.Test
{
    [TestClass]
    public class SigmaBuilderTests
    {
/*
        private SigmaTable sigma;

        private void CheckSigma(Vector v, int expectedK, int expectedP)
        {
            Assert.AreEqual(expectedK, sigma[v].K);
            Assert.AreEqual(expectedP, sigma[v].P);
        }

        [TestMethod]
        public void GivenW_SigmaBuilder_BuildsTable()
        {
            string parameters = "4\n2 3 4 5\n12\n2 3 4 1\n3 2 1 3\n";
            Parser parser = new Parser();

            KnapsackProblem problem = parser.Parse(parameters);
            WBuilder wBuilder = new WBuilder();
            VectorSet[,] w = wBuilder.BuildW(problem);

            SigmaBuilder sigmaBuilder = new SigmaBuilder();

            // Act (Действие)
            this.sigma = sigmaBuilder.Build(w, problem);

            // Assert (Проверка)
            CheckSigma(new Vector(2, 3), 1, 2);
            CheckSigma(new Vector(3, 2), 2, 3);
            CheckSigma(new Vector(5, 5), 2, 5);
            CheckSigma(new Vector(4, 1), 3, 4);
            CheckSigma(new Vector(6, 4), 3, 6);
            CheckSigma(new Vector(7, 3), 3, 7);
            CheckSigma(new Vector(9, 6), 3, 9);
            CheckSigma(new Vector(3, 6), 4, 7);
            CheckSigma(new Vector(6, 8), 4, 10);
            CheckSigma(new Vector(7, 7), 4, 11);
        }
*/
    }
}